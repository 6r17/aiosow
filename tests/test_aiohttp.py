import pytest
from stims.aiohttp import aiohttp_server
import asyncio

@pytest.mark.asyncio
async def test_app_factory():
    ''''''
    # task = aiohttp_server()
    # assert asyncio.iscoroutine(task)
